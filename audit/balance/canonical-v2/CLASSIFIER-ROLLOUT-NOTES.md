# Canonical V2 engine envelope classifier rollout

This rollout makes the canonical comparison read `effect.engineEffects`, `engineRequirements`, `engineChanges`, `engineCharges`, stack-scaled amounts, and the `other-enemies` target alias instead of classifying only legacy `mechanics`.
